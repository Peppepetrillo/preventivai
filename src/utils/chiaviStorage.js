import {
  APP_DATA_KEYS,
  BACKUP_DATA_KEYS,
  NATIVE_STORAGE_KEYS,
} from "../app/storageKeys";

/** Chiavi sync cloud + core dataset (invariante October). */
export const CHIAVI_DATI_APP = APP_DATA_KEYS;

/** Chiavi export/import backup locale (include operai, non espande sync cloud). */
export const CHIAVI_BACKUP = BACKUP_DATA_KEYS;

export const CHIAVI_STORAGE_NATIVO = NATIVE_STORAGE_KEYS;
