# PreventivAI — Night roadmap (excerpt)

## ✅ Preventivo → Cantiere

Done in product code. Hardened 2026-09-15 (events, CTA, busy lock, offline tests).

## Next (only if stable)

1. Varianti di cantiere — UX polish / tests if gaps remain (`domain/varianti` already exists).
2. Lint `set-state-in-effect` one page at a time.
3. Human PWA device QA.
