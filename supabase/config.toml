# PreventivAI — Supabase CLI
# Dopo `supabase link --project-ref <REF>`, la CLI aggiorna i metadati locali.
# Non inserire secret in questo file.

[api]
enabled = true

[db]
port = 54322

[studio]
enabled = true

# Sprint 22/23: JWT sessione obbligatorio (gateway + defense-in-depth nella function).
# L'OPENAI_API_KEY resta solo nei Secrets della function — mai nel client.
[functions.analisi-preventivo-intelligence]
verify_jwt = true
