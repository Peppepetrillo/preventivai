# Deploy Edge Function AI
# supabase functions deploy analisi-preventivo-intelligence
#
# Secrets (solo server — NON nel client / NON in VITE_*):
#   supabase secrets set OPENAI_API_KEY=<valore>
#   supabase secrets set OPENAI_MODEL=gpt-4o-mini
#   supabase secrets set ALLOWED_ORIGINS=https://tuo-dominio.com   # opzionale
#
# Client:
#   VITE_AI_ASSISTANT_ENDPOINT=https://<PROJECT-REF>.supabase.co/functions/v1/analisi-preventivo-intelligence
#
# PROJECT-REF: Dashboard → Project Settings → General → Reference ID
